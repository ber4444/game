name: Android Instrumentation Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  instrumented-tests:
    name: Instrumented Tests (API ${{ matrix.api-level }}, Device ${{ matrix.profile || matrix.target }})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - api-level: 24
            profile: "Nexus 5"
            target: google_apis
          - api-level: 29
            profile: "Pixel 4"
            target: google_apis
          - api-level: 30
            profile: "Pixel 5"
            target: google_apis
          - api-level: 33
            profile: "Pixel 7 Pro"
            target: google_apis

    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Set up JDK
        uses: actions/setup-java@v5.0.0
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v2

      - name: Show testing environment
        run: |
          echo "Running on API level: ${{ matrix.api-level }}"
          echo "Running on device: ${{ matrix.profile || matrix.target }}"

      - name: Build debug APK and test APK
        run: ./gradlew assembleDebug assembleAndroidTest

      - name: Run instrumentation tests
        uses: reactivecircus/android-emulator-runner@v2.34.0
        with:
          api-level: ${{ matrix.api-level }}
          target: ${{ matrix.target }}
          profile: ${{ matrix.profile }}
          arch: x86_64
          script: ./gradlew connectedDebugAndroidTest

      - name: Upload test results
        uses: actions/upload-artifact@v4.6.2
        with:
          name: test-results-api-${{ matrix.api-level }}-device-${{ matrix.profile || matrix.target }}
          path: app/build/reports/androidTests/connected/debug/index.html